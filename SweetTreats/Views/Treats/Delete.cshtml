@{
  Layout = "_Layout";
}

@model SweetTreats.Models.Treats

<h2>Are you sure you want to delete this treat?</h2>

@Html.DisplayNameFor(model => model.TreatsName): @Html.DisplayFor(model => model.TreatsName)
@using (Html.BeginForm())
{
    <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to treats", "Index")