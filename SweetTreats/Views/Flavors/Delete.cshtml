@{
  Layout = "_Layout";
}

@model SweetTreats.Models.Flavors

<h2>Are you sure you want to delete this flavor?</h2>

@Html.DisplayNameFor(model => model.FlavorsDescription): @Html.DisplayFor(model => model.FlavorsDescription)
@using (Html.BeginForm())
{
    <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to List", "Index")